#include "fixed.h"

// using namespace std;

 // constexpr explicit fixed(float x) 
 // :value((x/std::pow(2,Int) < 1 ? static_cast<underlying_type>(x *= 1 << fractionnal_part) : throw std::overflow_error("Can't do that."))) {
 
 // }
  //       {
 	// value = (x *= 1 << fractionnal_part);
 	// // std::cout<<"Constructor 1 is called"<<std::endl;
 // }


// int main(){


// 	return 0;
// }



